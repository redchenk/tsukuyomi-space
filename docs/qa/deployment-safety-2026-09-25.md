# 部署安全与后续修复验证

日期：2026-09-25。对应 2026-09-24 的项目分析后续工作。

## 已完成的改动

- 将国内服务器的对象存储上传补丁纳入源码，保留显式 `inline`、压缩、缓存和 OSS ACL 选项，默认上传行为不变。修复后的文件与服务器版本 SHA-256 相同：`74ecf603c41a4f745ac8eaf3c7d78b15323b405d9e6082338448d9f975ba4388`。
- 普通发布新增代码白名单、资源目录保护、服务器差异检查、资源内容及元数据校验。前端先添加新哈希文件，再原子替换首页；禁止覆盖不同内容的同名哈希文件及资源目录。
- 双站先预检再激活，验收 API、HTTP 首页和入口资产。失败时尝试恢复已经激活站点的代码、首页及原服务器补丁；保留海外 `frontend` 软链接和所有旧资源。回退拒绝覆盖更新的服务器提交或已被另行修改的首页。
- 自动代码回退不处理数据库和依赖环境，所以数据库迁移、锁文件和依赖/运行时要求变化需使用单独的环境发布；普通代码发布不会安装依赖或重写反向代理配置。
- 首页备份与已完成发布状态按国内/海外分别保留最近 10 份，清理与发布共用锁；待处理状态和非本程序创建的目录保留。
- 修复 Docker 对 `shared/` 的遗漏；模型、音乐、视频和音频通过只读资源挂载提供。构建依赖阶段提供 SQLite 源码编译工具，并在完整安装后裁剪开发依赖；镜像内代码权限保证非 root 用户可读、不可写。
- 审批仅改变审核状态，保留内容版本时间；同一摘要的重复批准保持幂等，内容被编辑后的旧摘要仍返回 409。

## 验证记录

在独立工作目录使用 Node **20.20.2**、npm **10.8.2** 和 `npm ci`，没有使用原工作目录中与锁文件不一致的依赖。

锁文件 SHA-256：`1ffc8c328596a8747e4f5d5da9197de747f71fa32729424ac2f64f8bb0763260`。

| 验证 | 结果 |
| --- | --- |
| `npm test` 语法检查 | 通过 |
| API 套件 | 206/206 |
| 消息安全与审核脚本 | 通过 |
| 邮箱与邮件模板 | 16/16 |
| 前端套件 | 227/227 |
| Playwright E2E（系统 Chrome、本地测试服务） | 52/52 |
| 中英文 web 构建 | 通过 |
| 部署故障模拟（临时 Git 仓库） | 12/12 |
| Git diff、Shell 语法和工作流 YAML | 通过 |
| Docker Linux 镜像构建与运行冒烟 | 通过；非 root、只读根文件系统下，健康、文章与首页接口均可访问 |

部署测试覆盖受保护路径删除、依赖变更拦截、Git 路径软链接、服务器补丁冲突、资源权限变化、哈希文件冲突、陈旧 HTTP 首页、更新版本保护、国内回退、海外失败回退与软链接保留、备份保留策略。审批测试将内容版本时间固定在过去，确定性覆盖跨秒重复批准，并验证两个审核入口都拒绝修改后的旧摘要。

Docker 最终镜像为 Linux amd64、Node 20.20.2，运行用户 `node`，镜像 ID 为 `sha256:ddd1f346f6dd452746a8c94aba0531a9b5ec397dbbc3f04e93eda98e0069a4f4`。验证发现并修复了原生 SQLite 编译工具缺失和非 root 代码读取权限两个实际问题。构建、冒烟和清理日志保存在本地忽略目录 `.codex_tmp/`。

收尾复核：两站首页 SHA-256 与分析时基线一致；国内 HEAD 和对象存储补丁未变。两站健康接口、Live2D 模型 JSON、Cubism 核心及音乐样本均 HTTP 200。海外既有两个 Docker 服务仍持续运行，测试容器、本次命名的验证镜像及临时目录已清理；共享 Docker 构建缓存未清理。海外 `frontend` 软链接仍指向原目录。

## 范围与现有告警

- 没有推送 Git、执行生产部署、重启现有服务或改写生产资源。国内 HEAD 仍为 `4908f765a8`；本地资源路径 `assets/`、`lib/`、`models/`、`live2d-core.js` 无 Git 改动。
- Docker 验证在海外机独立临时目录中进行。测试容器不挂载生产文件，也不发布端口。该服务器默认 Docker 构建网络出现 npm 域名解析失败，验证重试使用 `docker build --network=host`，没有修改宿主机 DNS 或 Docker daemon 配置。
- `npm ci` 提示已有锁定依赖 `sanitize-html@2.17.6` 需要 Node >=22.12.0、`utilium@3.4.0` 需要 Node >=22。Node 20 下上述应用测试通过，但不能据此视为依赖声明上的正式支持；线上 Node 版本本次未升级，运行时升级应单独安排和验证。
- 新流水线尚未在 GitHub Actions 上实际执行。跨服务器发布有短暂更新窗口，网络中断也可能使自动回退失败；持久化发布目录中的 `release.py` 可用于恢复操作。完整操作说明见 `docs/DEPLOY.md`。
